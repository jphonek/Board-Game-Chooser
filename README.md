Board Game chooser - PWA (React + Vite)
Files generated for testing locally. To run:

1. Install node (>=16). In project root run:
   npm install
   npm run dev

2. Build:
   npm run build
   npm run preview

This PWA stores data in localStorage (no server). It includes import/export CSV, player options, simple ads placeholder, sorting options, and results with adjusted times.
